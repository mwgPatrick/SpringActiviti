server:
  port: 8080
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: zhanghao
    password: mima
    url: jdbc:mysql://localhost:3306/test?character-set=utf-8
  activiti:
    database-schema-update: true
    history-level: audit
    process-definition-location-prefix: classpath:/processes/
  thymeleaf:
    mode: HTML
    cache: false
    encoding: UTF-8
#    prefix: classpath:templates/
#  mvc:
#    static-path-pattern: /static/**
#logging:
#  level: info
pagehelper:
  helperDialect: mysql
  reasonable: true
  supportMethodsArguments: true
  params: count=countSql